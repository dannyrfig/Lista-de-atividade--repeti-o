let soma = 0;
if(isNaN(numDez)){
    alert("Erro o número que você digitou não é válido");
} else {
    let num = parseInt(prompt(`Digite ${i} números para efetuar a soma deles `));

    for(let i= 0; i <= 10; i++ ){
        soma + i ;
        
    }
    console.log(`A soma dos números é equivalente á: ${soma}`);
}